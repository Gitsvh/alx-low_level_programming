#include "main.h"

/**
 * strlen - finds the length of a string
 * @s: string pointer to the string whose length is to be found
 * Reutrn: the length of the string
 */
int _strlen(char *s)
{
	int = 0;
	/ incremeant up to when the last character is null, \0/
		while (*(s + p) != 0)
		{
			p++;
		}
	return (p);
}
